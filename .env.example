# .env.example
# Renomeie este arquivo para .env e preencha com seus dados reais

# --- Configurações do Flask ---
# Gere uma chave aleatória segura. No terminal python: secrets.token_hex(16)
SECRET_KEY=troque_isso_por_uma_chave_aleatoria_e_segura

# --- Banco de Dados ---
# Exemplo para desenvolvimento local (SQLite):
DATABASE_URL=sqlite:///database.sqlite
# Exemplo para produção (PostgreSQL):
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/nome_do_banco

# --- Credenciais do Administrador ---
ADMIN_USERNAME=admin@exemplo.com.br

# A senha deve ser um HASH gerado pelo Werkzeug, não a senha em texto puro.
# Para gerar o hash, abra o terminal Python e rode:
# from werkzeug.security import generate_password_hash
# generate_password_hash("sua_senha_aqui")
# Copie o resultado abaixo:
ADMIN_PASSWORD_HASH=pbkdf2:sha256:260000$exemplo_de_hash_gerado_pelo_python